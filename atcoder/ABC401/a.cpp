#include <bits/stdc++.h>
using namespace std;

int main() {
    // ios::sync_with_stdio(false);
    // cin.tie(0);

    // Your code here
    int n;
    cin >> n;
    if (n >= 200 && n <= 299) {
        cout << "Success" << endl;
    } else {
        cout << "Failure" << endl;
    }
    return 0;
}